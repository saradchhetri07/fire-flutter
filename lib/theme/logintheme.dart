import 'package:flutter/material.dart';

class logintheme {
  static Color scaffoldTheme = Colors.black;
  static Color iconcolor = Color.fromRGBO(20, 20, 20, 1.0);
  static Color border = Colors.grey;
  static Color bordercolor = Color.fromRGBO(255, 0, 255, 2.0);

  static get boxcolor => LinearGradient(colors: [
    Colors.pink,
    Colors.pinkAccent
  ]);
}
